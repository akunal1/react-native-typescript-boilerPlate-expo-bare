import { StyleSheet } from 'react-native';

// Do not use hard coded values like bellow .Import from Theme file
export default StyleSheet.create({
  screen: { flex: 1, margin: 16 },
  header: { textAlign: 'center', fontSize: 22, padding: 16 },
  btnContainer: { flexDirection: 'row', justifyContent: 'space-evenly' },
  margin16: { margin: 16 },
});
